const Reader = require('./../../lib/utils/Reader');

describe("Unit Tests for Reader Class", () => {
    test('1) lee el archivo', () => {
        const result = 4+2;
        expect(result).toBe(6);
    });
})